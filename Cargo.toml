[workspace]
resolver = "2"
members = [ "ldk-server-cli", "ldk-server-client", "ldk-server-protos", "ldk-server"]
package.version = "0.1.0"

[profile.release]
panic = "abort"
opt-level = 3
lto = true

[profile.dev]
panic = "abort"

[workspace.metadata.crane]
name = "ldk-server-workspace"
